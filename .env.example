# JWT
JWT_SECRET=change-me-to-a-random-string

# 初始管理员
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me

# 数据库：默认用容器内的 SQLite 文件（配合 docker-compose.yml 里的具名 volume
# 持久化），单机自托管场景不需要额外配置，一般不用改这一项。
# DATABASE_URL=sqlite:///./data/imaotai.db
